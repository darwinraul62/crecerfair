button(type="button", class="btn btn-primary btn-sm d-none d-lg-block", data-bs-toggle="collapse", data-bs-target="#style-switch", id="style-switch-button")
    i.fa.fa-cog.fa-2x


#style-switch.collapse
    .p-3
        h6.text-uppercase.mb-4 Select theme colour

        form.mb-3
            select.form-select.style-switch-select(name="colour",id="colour")
                option(value="") select colour variant
                option(value=path + "css/style.default.css") blue
                option(value=path + "css/style.pink.css") pink
                option(value=path + "css/style.green.css") green
                option(value=path + "css/style.red.css") red
                //- option(value="sea") sea
                option(value=path + "css/style.violet.css") violet

        p.text-muted.text-sm Stylesheet switching is done with JavaScript and can cause a blink while page loads. This will not happen in your production code.
          